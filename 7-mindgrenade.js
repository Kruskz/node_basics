const num1 = 5;
const num2 = 10;

function addVal(){
    console.log(`the sum is ${num1+num2}`)
}

addVal()

//require('./7-mindgrenade') putting this in index js without assigning 
//a constant to it will run the function because we are calling it in our file
